pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven {
            url "https://maven.bookmc.org"
        }
    }
}

rootProject.name = "buildtools"

